---
import { AiOutlineHome } from "react-icons/ai";
import Layout from "../layouts/Layout.astro";
import { FaArrowLeft } from "react-icons/fa";
---

<Layout>
	<main
		class="min-h-screen flex flex-col items-center justify-center p-4 text-secondary overflow-hidden relative"
	>
		<div class="relative z-10 text-center max-w-2xl">
			<div class="text-9xl font-bold mb-4 flex justify-center">
				<span class="text-secondary transform rotate-12 inline-block"
					>4</span
				>
				<span
					class="text-secondary transform -rotate-12 inline-block mx-2"
					>0</span
				>
				<span class="text-secondary transform rotate-12 inline-block"
					>4</span
				>
			</div>

			<h1 class="text-4xl font-bold mb-6">¡Ups! Página no encontrada</h1>

			<p class="text-xl mb-8 opacity-90 text-black">
				Lo sentimos, la página que estás buscando no existe o ha sido
				movida.
			</p>

			<div
				class="space-y-4 sm:space-y-0 sm:space-x-4 sm:flex sm:justify-center grid grid-cols-1 sm:grid-cols-2"
			>
				<button
					onclick="window.history.back()"
					class="cursor-pointer px-6 py-3 bg-secondary text-white font-semibold rounded-lg shadow-lg hover:bg-secondary/90 transition-all duration-300 ease-in-out transform hover:-translate-y-1 flex items-center justify-center"
				>
					<span class="text-base mr-2">
						<FaArrowLeft />
					</span>
					Volver atrás
				</button>

				<button
					onclick="window.location.href = '/'"
					class="cursor-pointer px-6 py-3 bg-secondary border-2 border-white text-white font-semibold rounded-lg shadow-lg hover:bg-secondary/90 hover:text-white transition-all duration-300 ease-in-out transform hover:-translate-y-1 flex items-center justify-center"
				>
					<span class="text-lg mr-2">
                        <AiOutlineHome />
                    </span>
					Ir al inicio
				</button>
			</div>
		</div>
	</main>
</Layout>
